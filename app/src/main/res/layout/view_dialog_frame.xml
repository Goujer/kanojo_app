<?xml version="1.0" encoding="utf-8"?>
<merge xmlns:android="http://schemas.android.com/apk/res/android"
	xmlns:tools="http://schemas.android.com/tools"
	tools:parentTag="android.widget.RelativeLayout">
	<RelativeLayout
		android:layout_width="match_parent"
		android:layout_height="match_parent"
		android:background="@drawable/dialog_frame">
		<ImageButton
			android:id="@+id/dialog_close_btn"
			android:layout_width="40dp"
			android:layout_height="40dp"
			android:layout_alignParentTop="true"
			android:layout_alignParentRight="true"
			android:background="@drawable/dialog_btn_close"
			android:contentDescription="@string/kanojo_info_close" />
		<TextView
			android:id="@+id/dialog_view_message"
			android:layout_width="match_parent"
			android:layout_height="wrap_content"
			android:layout_above="@+id/dialog_site_btn"
			android:layout_below="@+id/dialog_close_btn"
			android:layout_centerHorizontal="true"
			android:gravity="center_horizontal"
			android:padding="10dp"
			android:singleLine="false"
			android:textColor="@android:color/black" />
		<Button
			android:id="@+id/dialog_site_btn"
			android:layout_width="@dimen/kanojo_room_dialog_site_width"
			android:layout_height="@dimen/kanojo_room_dialog_site_height"
			android:layout_alignParentBottom="true"
			android:layout_alignParentRight="true"
			android:layout_centerHorizontal="true"
			android:layout_marginBottom="15dp"
			android:layout_marginRight="15dp"
			android:background="@color/kanojo_room_dialog_status_bg"
			android:gravity="center"
			android:text="@string/button_next"/>
	</RelativeLayout>
</merge>
